import { ArchivepipePipe } from './archivepipe.pipe';

describe('ArchivepipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArchivepipePipe();
    expect(pipe).toBeTruthy();
  });
});
